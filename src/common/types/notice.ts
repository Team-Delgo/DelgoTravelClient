export interface PlaceNoticeType {
  contents: Array<string>;
  placeId: number;
  placeNoticeId: number;
  title: string;
}

export interface RoomNoticeType {
  contents: Array<string>;
  roomId: number;
  roomNoticeId: number;
  title: string;
}
