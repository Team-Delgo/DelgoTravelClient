export interface EditorImgType {
  editorNoteId: number;
  order: number;
  placeId: number;
  thumbnailUrl: string;
  url: string;
}

export interface EditorPlaceType {
  mainUrl: string;
  placeId: number;
  thumbnailSubtitle: string;
  thumbnailTitle: string;
  thumbnailUrl: string;
}
